node_modules/
.DS_Store

# Build scratch: the pinned Vencord checkout and the assembled mod bundle.
# Reproducible from vencord.pin.json plus the plugin sources, so it is an
# output, not a source.
installer/build/

# Compiled TypeScript. Produced by `pnpm build:app`; sources are in installer/src.
installer/dist/

# electron-builder output.
installer/release/

# Agent tooling scratch — never part of the product.
.superpowers/
